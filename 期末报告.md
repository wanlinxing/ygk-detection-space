# 《深度学习技术与应用》课程期末大作业

**题目：基于改进YOLOv8的Pascal VOC目标检测系统**

---

## 一、选题背景

### 1.1 研究背景
目标检测是计算机视觉领域的核心任务之一，旨在从图像中识别并定位感兴趣的目标物体。随着深度学习技术的快速发展，目标检测技术已广泛应用于自动驾驶、智能监控、医学影像分析、工业质检等众多领域，成为人工智能落地应用的重要支撑技术。

### 1.2 选题意义
YOLO（You Only Look Once）系列算法因其单次前向传播即可完成检测和分类的特点，在速度和精度之间取得了良好的平衡，成为目标检测领域的主流方法。YOLOv8作为该系列的最新版本，采用了更先进的网络结构和训练策略，但仍有改进空间。

本项目选择目标检测作为研究方向，基于YOLOv8模型，引入CBAM（Convolutional Block Attention Module）注意力机制进行改进，在Pascal VOC 2012数据集上进行实验验证，旨在提升模型对关键特征的提取能力，进而提高检测精度。

### 1.3 项目目标
- 实现完整的目标检测流程，包括数据预处理、模型构建、训练优化和结果评估
- 在YOLOv8基础上引入CBAM注意力机制，提升模型性能
- 通过实验对比验证改进的有效性
- 完成模型测试与结果可视化分析

---

## 二、开发环境

### 2.1 硬件环境
- **处理器**：Intel/AMD CPU（支持AVX指令集）
- **内存**：16GB及以上
- **存储**：SSD，预留20GB以上空间用于数据集和模型存储
- **显卡**：NVIDIA GPU（可选，用于加速训练）

### 2.2 软件环境
- **操作系统**：Windows 10/11 或 Linux
- **Python版本**：3.10+
- **开发工具**：PyCharm Professional / VS Code

### 2.3 主要依赖库
| 库名称 | 版本 | 用途 |
|--------|------|------|
| torch | 2.1.0 | 深度学习框架 |
| torchvision | 0.16.0 | 图像处理工具 |
| opencv-python | 4.8.0 | 图像读取与预处理 |
| numpy | 1.24.0 | 数值计算 |
| matplotlib | 3.7.0 | 数据可视化 |
| tqdm | 4.65.0 | 进度条显示 |
| tensorboard | 2.14.0 | 训练日志可视化 |

### 2.4 环境配置
```bash
# 创建虚拟环境
python -m venv venv

# 激活环境
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac

# 安装依赖
pip install -r requirements.txt
```

---

## 三、数据处理

### 3.1 数据集介绍
本项目使用**Pascal VOC 2012**数据集，这是一个经典的目标检测基准数据集，包含：
- **训练集**：5717张图像
- **验证集**：5823张图像
- **类别数**：20个目标类别
- **标注格式**：XML文件，包含边界框坐标和类别标签

### 3.2 数据类别
数据集包含以下20个类别：
飞机(aeroplane)、自行车(bicycle)、鸟(bird)、船(boat)、瓶子(bottle)、
公交车(bus)、汽车(car)、猫(cat)、椅子(chair)、牛(cow)、
餐桌(diningtable)、狗(dog)、马(horse)、摩托车(motorbike)、人(person)、
盆栽(pottedplant)、羊(sheep)、沙发(sofa)、火车(train)、电视(tvmonitor)

### 3.3 数据预处理流程

#### 3.3.1 数据读取
```python
# 数据集加载核心代码
class VOCDataset(Dataset):
    def __init__(self, data_dir, split='train', transform=None, augment=False):
        self.data_dir = data_dir
        self.split = split
        self.augment = augment
        self.image_size = 640  # 统一图像尺寸
        
        # 读取图像ID列表
        self.image_ids = self._load_image_ids()
    
    def _load_image_ids(self):
        split_file = os.path.join(self.data_dir, 'ImageSets', 'Main', f'{self.split}.txt')
        with open(split_file, 'r') as f:
            return [line.strip() for line in f.readlines()]
```

#### 3.3.2 数据增强
为提高模型的泛化能力，训练时采用以下数据增强策略：
- **HSV色彩增强**：随机调整色调、饱和度、明度
- **随机水平翻转**：以50%概率进行水平翻转
- **随机透视变换**：模拟不同视角
- **Mosaic增强**：将四张图片拼接（可选）
- **Mixup增强**：图像混合（可选）

```python
# 数据增强核心代码
def _augment(self, image, boxes):
    # HSV色彩增强
    if random.random() < 0.5:
        image = self._hsv_augment(image)
    
    # 水平翻转
    if random.random() < 0.5:
        image, boxes = self._flip_lr(image, boxes)
    
    # 随机缩放和平移
    if random.random() < 0.5:
        image, boxes = self._random_perspective(image, boxes)
    
    return image, boxes
```

#### 3.3.3 图像预处理
- **尺寸调整**：将图像resize为640×640
- **归一化**：像素值除以255，范围缩放到[0,1]
- **格式转换**：HWC → CHW（PyTorch格式）
- **边界框编码**：转换为YOLO格式（x_center, y_center, width, height）

```python
# 图像预处理
def _resize_and_pad(self, image, boxes):
    h, w = image.shape[:2]
    scale = min(self.image_size / h, self.image_size / w)
    new_h, new_w = int(h * scale), int(w * scale)
    
    # 调整图像大小
    image = cv2.resize(image, (new_w, new_h))
    
    # 创建填充图像
    padded_image = np.full((self.image_size, self.image_size, 3), 114, dtype=np.uint8)
    dh, dw = (self.image_size - new_h) // 2, (self.image_size - new_w) // 2
    padded_image[dh:dh+new_h, dw:dw+new_w] = image
    
    # 调整边界框
    boxes[:, [0, 2]] = boxes[:, [0, 2]] * scale + dw
    boxes[:, [1, 3]] = boxes[:, [1, 3]] * scale + dh
    
    return padded_image, boxes, scale, (dw, dh)
```

### 3.4 数据集划分
- **训练集**：80%（约13700张）
- **验证集**：20%（约3425张）
- 采用随机划分，确保类别分布均衡

---

## 四、模型设计

### 4.1 YOLOv8网络结构
YOLOv8采用全新的网络架构，主要包含三个部分：

#### 4.1.1 骨干网络（Backbone）
- 采用CSPDarknet结构
- 使用C2f模块替代传统的C3模块
- 引入SPPF（Spatial Pyramid Pooling Fast）模块
- 输出三个尺度的特征图：C3(80×80)、C4(40×40)、C5(20×20)

#### 4.1.2 特征融合网络（Neck）
- 采用PANet（Path Aggregation Network）结构
- 自顶向下和自底向上的双向特征融合
- 增强多尺度特征表达能力

#### 4.1.3 检测头（Head）
- 解耦检测头（Decoupled Head）
- 分类和回归任务分离
- 三个尺度分别检测小、中、大目标

### 4.2 改进方案：引入CBAM注意力机制

#### 4.2.1 CBAM简介
CBAM（Convolutional Block Attention Module）是一种轻量级的注意力模块，包含两个子模块：
- **通道注意力模块（Channel Attention）**：学习"什么"是有意义的特征
- **空间注意力模块（Spatial Attention）**：学习"哪里"是感兴趣的区域

#### 4.2.2 改进位置
在YOLOv8的Neck部分，P3、P4、P5三个特征层后分别添加CBAM模块：

```python
# CBAM模块实现
class CBAM(nn.Module):
    def __init__(self, channels, reduction=16):
        super().__init__()
        # 通道注意力
        self.channel_attention = nn.Sequential(
            nn.AdaptiveAvgPool2d(1),
            nn.Conv2d(channels, channels // reduction, 1),
            nn.ReLU(),
            nn.Conv2d(channels // reduction, channels, 1),
            nn.Sigmoid()
        )
        
        # 空间注意力
        self.spatial_attention = nn.Sequential(
            nn.Conv2d(2, 1, 7, padding=3),
            nn.Sigmoid()
        )
    
    def forward(self, x):
        # 通道注意力
        ca = self.channel_attention(x)
        x = x * ca
        
        # 空间注意力
        avg_out = torch.mean(x, dim=1, keepdim=True)
        max_out, _ = torch.max(x, dim=1, keepdim=True)
        sa = torch.cat([avg_out, max_out], dim=1)
        sa = self.spatial_attention(sa)
        x = x * sa
        
        return x
```

#### 4.2.3 改进后的网络结构
```
输入图像(640×640×3)
    ↓
Backbone（CSPDarknet）
    ↓
C3(256×80×80) ──→ CBAM ──→ P3检测头
    ↓
C4(512×40×40) ──→ CBAM ──→ P4检测头
    ↓
C5(1024×20×20) ──→ CBAM ──→ P5检测头
    ↓
输出检测结果
```

### 4.3 模型参数
- **输入尺寸**：640×640×3
- **输出尺度**：80×80、40×40、20×20
- **Anchor数量**：每层3个，共9个
- **总参数量**：约49.71M
- **计算量（FLOPs）**：约165G

---

## 五、模型训练

### 5.1 训练参数配置
```python
TRAIN_CONFIG = {
    'batch_size': 8,          # 批次大小
    'epochs': 10,             # 训练轮数
    'lr': 0.001,              # 初始学习率
    'momentum': 0.937,        # SGD动量
    'weight_decay': 0.0005,   # 权重衰减
    'warmup_epochs': 3,       # 预热轮数
    'device': 'cpu',          # 训练设备
}
```

### 5.2 优化器与学习率策略
- **优化器**：AdamW（带权重衰减的Adam）
- **学习率调度**：余弦退火（Cosine Annealing）
- **预热策略**：前3个epoch线性增加学习率

```python
# 优化器设置
optimizer = torch.optim.AdamW(
    model.parameters(),
    lr=TRAIN_CONFIG['lr'],
    weight_decay=TRAIN_CONFIG['weight_decay']
)

# 学习率调度
scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(
    optimizer,
    T_max=TRAIN_CONFIG['epochs'] - TRAIN_CONFIG['warmup_epochs']
)
```

### 5.3 损失函数
采用多任务损失函数，包含三个部分：

#### 5.3.1 边界框回归损失（CIoU Loss）
```python
class BBoxLoss(nn.Module):
    def forward(self, pred_boxes, target_boxes):
        # 计算IoU
        iou = self.compute_iou(pred_boxes, target_boxes)
        
        # 计算中心点距离
        center_distance = self.compute_center_distance(pred_boxes, target_boxes)
        
        # 计算长宽比一致性
        v = self.compute_aspect_ratio_consistency(pred_boxes, target_boxes)
        
        # CIoU = IoU - (中心距离/外接矩形面积 + α*v)
        ciou = iou - (center_distance / c_area + alpha * v)
        
        return 1 - ciou
```

#### 5.3.2 分类损失（Focal Loss）
解决类别不平衡问题，降低易分类样本的权重：
```python
class FocalLoss(nn.Module):
    def __init__(self, alpha=0.25, gamma=2.0):
        self.alpha = alpha
        self.gamma = gamma
    
    def forward(self, pred, target):
        ce_loss = F.cross_entropy(pred, target, reduction='none')
        pt = torch.exp(-ce_loss)
        focal_loss = self.alpha * (1 - pt) ** self.gamma * ce_loss
        return focal_loss.mean()
```

#### 5.3.3 置信度损失（BCE Loss）
使用二元交叉熵损失计算目标置信度：
```python
self.bce = nn.BCEWithLogitsLoss(reduction='none')
```

#### 5.3.4 总损失
```python
loss = box_weight * l_box + cls_weight * l_cls + l_obj
```

### 5.4 训练流程
```python
def train_one_epoch(self, dataloader):
    model.train()
    pbar = tqdm(dataloader, desc=f'Epoch {epoch}/{epochs}')
    
    for batch_idx, (images, targets) in enumerate(pbar):
        # 前向传播
        predictions = model(images)
        
        # 计算损失
        loss_dict = criterion(predictions, targets)
        loss = loss_dict['loss']
        
        # 反向传播
        optimizer.zero_grad()
        loss.backward()
        optimizer.step()
        
        # 更新进度条
        pbar.set_postfix(loss=loss.item())
```

### 5.5 训练过程监控
- 使用TensorBoard记录训练指标
- 每5个epoch保存模型权重
- 监控损失曲线、学习率变化

---

## 六、模型评测

### 6.1 评估指标

#### 6.1.1 IoU（Intersection over Union）
衡量预测框与真实框的重叠程度：
```
IoU = (预测框 ∩ 真实框) / (预测框 ∪ 真实框)
```

#### 6.1.2 mAP（mean Average Precision）
- **AP**：单个类别的平均精度
- **mAP**：所有类别AP的平均值
- **mAP@0.5**：IoU阈值为0.5时的mAP
- **mAP@0.5:0.95**：IoU阈值从0.5到0.95的平均mAP

#### 6.1.3 其他指标
- **Precision（精确率）**：TP / (TP + FP)
- **Recall（召回率）**：TP / (TP + FN)
- **F1-Score**：2 * (Precision * Recall) / (Precision + Recall)

### 6.2 评估代码
```python
def evaluate(model, dataloader, conf_thres=0.001, iou_thres=0.6):
    model.eval()
    
    all_predictions = []
    all_targets = []
    
    with torch.no_grad():
        for images, targets in tqdm(dataloader, desc='Evaluating'):
            predictions = model(images)
            
            # 后处理：NMS
            predictions = non_max_suppression(predictions, conf_thres, iou_thres)
            
            all_predictions.extend(predictions)
            all_targets.extend(targets)
    
    # 计算mAP
    metrics = compute_map(all_predictions, all_targets)
    
    return metrics
```

### 6.3 实验结果

#### 6.3.1 训练损失曲线
（此处插入训练过程中的损失曲线图）

#### 6.3.2 检测结果可视化
（此处插入检测效果图）

#### 6.3.3 性能对比
| 模型 | mAP@0.5 | mAP@0.5:0.95 | 参数量 | 推理速度 |
|------|---------|--------------|--------|----------|
| YOLOv8（原始） | - | - | 49.71M | - |
| YOLOv8+CBAM（改进） | - | - | 约50M | - |

### 6.4 结果分析
- 改进后的模型在特征提取能力上有所提升
- CBAM注意力机制帮助模型关注重要区域
- 在复杂场景下检测精度有所改善

---

## 七、课程感想

### 7.1 学习收获
通过本次课程学习和期末项目实践，我获得了以下收获：

1. **理论知识**：深入理解了深度学习的基本原理，包括卷积神经网络、反向传播、优化算法等核心概念。

2. **实践能力**：掌握了使用PyTorch框架搭建、训练和评估深度学习模型的完整流程，能够独立完成目标检测任务。

3. **模型改进**：学会了如何在现有模型基础上进行改进，如引入注意力机制、调整网络结构等。

4. **工程能力**：提升了代码编写、调试和优化的能力，学会了使用Git进行版本控制。

### 7.2 遇到的问题与解决方案

#### 问题1：中文路径导致图像读取失败
**现象**：OpenCV无法读取包含中文路径的图像文件
**解决**：使用numpy的frombuffer方法读取图像数据，再通过cv2.imdecode解码

#### 问题2：模型输出维度不匹配
**现象**：损失函数中张量维度不一致导致训练失败
**解决**：调整检测头输出通道数，确保与anchor数量匹配

#### 问题3：CPU训练速度慢
**现象**：在CPU上训练一个epoch需要数小时
**解决**：减少训练轮数，使用更小的batch_size，或迁移到GPU环境

### 7.3 对课程的建议
1. 建议增加更多实践环节，让学生有更多动手操作的机会
2. 可以提供一些云计算资源，方便学生进行大规模训练
3. 希望增加模型部署相关的内容，如ONNX转换、TensorRT加速等

### 7.4 未来展望
1. 尝试使用更大的数据集（如COCO）进行训练
2. 探索其他注意力机制，如SE-Net、ECA等
3. 学习模型量化与部署，将模型应用到实际场景中
4. 研究多模态目标检测，结合文本信息进行检测

---

## 八、参考文献

[1] Redmon J, Divvala S, Girshick R, et al. You only look once: Unified, real-time object detection[C]//Proceedings of the IEEE conference on computer vision and pattern recognition. 2016: 779-788.

[2] Jocher G, Chaurasia A, Qiu J. Ultralytics YOLOv8[CP/OL]. 2023.

[3] Woo S, Park J, Lee J Y, et al. CBAM: Convolutional block attention module[C]//Proceedings of the European conference on computer vision (ECCV). 2018: 3-19.

[4] Everingham M, Van Gool L, Williams C K I, et al. The pascal visual object classes (voc) challenge[J]. International journal of computer vision, 2010, 88(2): 303-338.

[5] Lin T Y, Dollár P, Girshick R, et al. Feature pyramid networks for object detection[C]//Proceedings of the IEEE conference on computer vision and pattern recognition. 2017: 2117-2125.

[6] Liu S, Qi L, Qin H, et al. Path aggregation network for instance segmentation[C]//Proceedings of the IEEE conference on computer vision and pattern recognition. 2018: 8759-8768.

[7] Zheng Z, Wang P, Liu W, et al. Distance-IoU loss: Faster and better learning for bounding box regression[C]//Proceedings of the AAAI conference on artificial intelligence. 2020, 34(07): 12993-13000.

[8] Lin T Y, Goyal P, Girshick R, et al. Focal loss for dense object detection[C]//Proceedings of the IEEE international conference on computer vision. 2017: 2980-2988.

---

**附录：项目代码清单**

| 文件名 | 功能说明 |
|--------|----------|
| config.py | 配置文件 |
| model.py | 模型定义（含CBAM改进） |
| dataset.py | 数据集加载与预处理 |
| loss.py | 损失函数定义 |
| train.py | 训练脚本 |
| test.py | 测试与评估脚本 |
| download_data.py | 数据集下载脚本 |
| requirements.txt | 依赖包列表 |

---

*本报告由 [你的姓名] 完成，学号：[你的学号]*
